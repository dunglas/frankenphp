before:
  hooks:
    - go mod download
env:
  - CGO_ENABLED=1
  - CGO_LDFLAGS=-L/usr/local/lib -L/usr/local/opt/libiconv/lib -L/usr/lib -lphp -lxml2 -lresolv -lsqlite3 -ldl -lm -lutil -liconv
  - CGO_CFLAGS=-Wall -I/usr/local/include/php -I/usr/local/include/php/Zend -I/usr/local/include/php/TSRM -I/usr/local/include/php/main
builds:
  - id: frankenphp
    dir: caddy/frankenphp
    binary: frankenphp
    goos:
      - darwin
    goarch:
      - amd64
archives:
  - id: frankenphp
    replacements:
      darwin: Darwin
